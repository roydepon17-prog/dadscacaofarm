"use client";
import { FormEvent, useState } from "react";
export default function InquiryForm() { const [status,setStatus]=useState<"idle"|"success"|"error">("idle"); const [msg,setMsg]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const data=Object.fromEntries(f); if(!data.name||!data.email||!data.message){setStatus("error");setMsg("Please complete all fields.");return;} try {const r=await fetch("/api/inquiry",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error);setStatus("success");setMsg(j.message);e.currentTarget.reset();}catch{setStatus("error");setMsg("We could not send this now. Please email dadscacaofarm@gmail.com.");}} return <form onSubmit={submit} className="grid gap-4"><label className="grid gap-1 text-xs font-bold">Name<input required name="name" className="h-12 rounded-lg border border-[#d9cdb7] bg-parchment px-3 text-sm font-normal"/></label><label className="grid gap-1 text-xs font-bold">Email<input required name="email" type="email" className="h-12 rounded-lg border border-[#d9cdb7] bg-parchment px-3 text-sm font-normal"/></label><label className="grid gap-1 text-xs font-bold">Message<textarea required name="message" rows={4} className="rounded-lg border border-[#d9cdb7] bg-parchment p-3 text-sm font-normal"/></label><button className="min-h-11 rounded-lg bg-gold px-5 text-sm font-extrabold text-cocoa">Send inquiry</button>{status!=="idle"&&<p role="status" className={status==="success"?"text-sm text-canopy":"text-sm text-red-700"}>{msg}</p>}<a className="text-center text-xs font-bold underline" href="mailto:dadscacaofarm@gmail.com">Email instead</a></form>; }
